version: "3.9"

services:
  client:
    build:
      context: .
      dockerfile: Dockerfile.client
    container_name: gmr_scrap_client
    environment:
      NODE_ENV: development
    ports:
      - "4200:4200"
    networks:
      - app-network
  server:
    build:
      context: .
      dockerfile: Dockerfile.server
    container_name: gmr_scrap_server
    working_dir: /usr/src/app
    volumes:
      - ./server:/usr/src/app/server
      - ./firebase.json:/usr/src/app/firebase.json
      - ./firebasekeys.json:/usr/src/app/firebasekeys.json
    environment:
      NODE_ENV: development
    ports:
      - "1337:1337"
    networks:
      - app-network
  machine:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: gmr_scrap_machine
    working_dir: /usr/src/app
    volumes:
      - ./machine:/usr/src/app/machine
      - ./firebase.json:/usr/src/app/firebase.json
      - ./firebasekeys.json:/usr/src/app/firebasekeys.json
    environment:
      NODE_ENV: development

networks:
  app-network:
    driver: bridge
